# flask-crud-app

Simple Flask CRUD app using SQLite and SQLAlchemy.
Run:
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python run.py
```